# kiruk-templates/contract

Local spec stub. Authoritative: `openspec/specs/template-contract/spec.md`.

**Important**: all generator output is NOT legal advice and MUST be reviewed by a lawyer before signing. Front-matter flag `legal-review-required: true` is mandatory.

Run via `/kiruk-artifact contract`.
