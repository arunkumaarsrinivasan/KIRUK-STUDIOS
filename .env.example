# Copy to .env or .env.local (never commit real values).
# See openspec/specs/repo-privacy/spec.md and CONTRIBUTING.md.

# Example placeholders — replace with real values only in ignored local files:
# DATABASE_URL=
# API_URL=
# SOME_SERVICE_API_KEY=
