---
interface Props {
  href: string;
  label: string;
  class?: string;
}
const { href, label, class: cls = '' } = Astro.props;
---

<a href={href} class={`sketch-button handwritten text-lg ${cls}`}>
  <slot name="lead" />
  <span>{label}</span>
  <span aria-hidden="true">&rarr;</span>
</a>
