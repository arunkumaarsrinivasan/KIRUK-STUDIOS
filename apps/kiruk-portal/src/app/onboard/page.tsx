import OnboardingWizard from '@/components/OnboardingWizard';

export const metadata = {
  title: 'Onboard a client — kiruk portal',
};

export default function OnboardPage() {
  return <OnboardingWizard />;
}
